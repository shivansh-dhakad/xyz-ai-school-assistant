// Portal configuration for principal-portal.
// Point this at wherever the shared "05. XYZ AI Repository/xyz-ai" backend
// is deployed if it is not served from the same origin as this portal.
const API_BASE = "/api";
const PORTAL_ROLE = "principal";
